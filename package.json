{
  "name": "oneline-room-card",
  "version": "1.3.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "npm run check:syntax && npm run check:dist && npm test",
    "check:dist": "node scripts/check-release.mjs",
    "check:syntax": "node --check src/room-card.js && node --check dist/room-card.js",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
